ARCH_DIRS =
if OPT_SSE
  ARCH_DIRS += sse
endif
if OPT_SSE2
  ARCH_DIRS += sse2
endif
if OPT_440
  ARCH_DIRS += 440
endif
if OPT_440d
  ARCH_DIRS += 440d
endif
if OPT_QPX
  ARCH_DIRS += qpx
endif

SUBDIRS = c $(ARCH_DIRS)

